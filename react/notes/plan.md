# Week Plan — Kick With Reverb v2

## Monday — Foundation
- Read react.dev: describing-the-ui, adding-interactivity
- Learn AI skills, PRs with AI, Claude Code
- Set up Django project + PostgreSQL (Neon or Docker)
- Verify React can call a test endpoint

## Tuesday — Models + Refactor
- Read react.dev: managing-state, escape-hatches
- Refactor Daw.tsx into custom hooks

## Wednesday — Auth
- Define Django models (User, Preset) + run migrations
- Django REST Framework auth endpoints (signup, login, JWT)
- React auth context + Login/Signup UI
- Test full auth flow

## Thursday — Presets CRUD
- Preset endpoints: list, create, load, delete
- React UI: save/load presets

## Friday — Kick Generation
- POST /generate-kick endpoint
- Option A: Self-host HuggingFace model
- Option B: Replicate API (fallback)
- Connect to React

## Saturday — Buffer
- Catch up, fix bugs, polish UI
- Redeploy with backend

## Sunday — Send
